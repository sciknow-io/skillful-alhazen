[defaults]
# Silence deprecation warnings
deprecation_warnings = False
# Disable automatic injection of facts as top-level variables (Modern Ansible behavior)
inject_facts_as_vars = False
# Use the local inventory
inventory = inventory.ini
# Host key checking disabled for easier initial setup
host_key_checking = False
# Standardize python interpreter and silence warnings
interpreter_python = auto_silent
