{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "REPO=$(git rev-parse --show-toplevel 2>/dev/null) && [ -f \"$REPO/local_resources/skilllog/skill_logger.py\" ] && cd \"$REPO\" && uv run python \"$REPO/local_resources/skilllog/skill_logger.py\" || true"
          }
        ]
      }
    ]
  }
}
